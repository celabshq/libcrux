# AVX2 load_block forall-25 cascade — diagnosis & fix (2026-05-05)

## Phase 1: Confirmed regression
- `Libcrux_sha3.Simd.Avx2.fst` is untracked (regenerated by hax extract).
- `Libcrux_sha3.Simd.Avx2.fst.checked` deleted; rebuild trips the cliff.
- Baseline `make check/Libcrux_sha3.Simd.Avx2.fst` (rlimit 400, split_queries always)
  shows ~5 failing sub-queries on `load_block` body (qs 693, 694, 695, 696, 797, 902),
  each canceling at 400/400 in ~100s.
- 3 unique source positions: lines 1091:20, 1164:20, 1249:20 (the 4 per-iteration
  hax_lib::assert calls in avx2.rs:275-290 — `load_block` per-iteration block-of-4
  lane equality asserts at indices 4i, 4i+1, 4i+2, 4i+3).
- Treating as regression (Arm64 had identical cliff resolved 2026-05-05 by
  opacifying `createi`). No changes to load_block source.

## Phase 2 — TODO: profiling, fix.
