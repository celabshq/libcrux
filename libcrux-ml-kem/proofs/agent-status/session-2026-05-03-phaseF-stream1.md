# Phase F Stream 1 — Lane E unpacked-API close — Session Report

**Date:** 2026-05-03
**Branch:** `agent-mlkem/phase-f-stream1-ind_cca`
**Worktree:** `/Users/karthik/libcrux-stream1-ind_cca`
**Parent SHA:** `b9eee5838` (Phase E push 3)

## Goal
Drop `Libcrux_ml_kem.Ind_cca.Unpacked.fst` from `ADMIT_MODULES` in `libcrux-ml-kem/proofs/fstar/extraction/Makefile` by flipping `unpacked::encapsulate` and `unpacked::decapsulate` (in `libcrux-ml-kem/src/ind_cca.rs`) to `panic_free` honestly (no admit-shuffling). The previous attempt at the drop was reverted at SHA `685191caf` because it was admit-shuffling; this time it must be real verification end-to-end.

## Per-fn flip table

| Function | Before | After | Notes |
|---|---|---|---|
| `unpacked::encapsulate` | default-verify (Unpacked admitted) | `panic_free` | No new requires; encrypt_unpacked already required no extra bound. Body verifies in <1s. |
| `unpacked::decapsulate` | default-verify (Unpacked admitted) | `panic_free` | Added `is_bounded_polynomial_vector(3328, &key_pair.private_key.ind_cpa_private_key.secret_as_ntt)` to requires (forwarded to decrypt_unpacked). Bumped to `--z3rlimit 400 --split_queries always` (fsti subtyping check on the decapsulate ensures was timing out at rlimit 80). Body verifies in 77s. |

The `panic_free` mode emits a single `let _:Prims.unit = admit () (* Panic freedom *) in result` at the end of each function — this is the standard hax pattern that admits the strong functional ensures while fully verifying the body. It is NOT new admit-shuffling (per memory note: panic_free IS the real middle step).

## Caller-side requires propagation (R-rule R9 honest)

Adding the bound to `unpacked::decapsulate` broke the call sites; I propagated honestly:

| File | Function | Change |
|---|---|---|
| `src/ind_cca/instantiations.rs` | `decapsulate` | Appended `Libcrux_ml_kem.Polynomial.Spec.is_bounded_polynomial_vector $K (mk_usize 3328) ${key_pair}.f_private_key.f_ind_cpa_private_key.f_secret_as_ntt` to the F* requires fragment. |
| `src/ind_cca/instantiations/avx2.rs` | `decapsulate_avx2` (unsafe inner) | Same. |
| `src/ind_cca/instantiations/avx2.rs` | `decapsulate` (outer wrapper) | Same. |
| `src/mlkem512.rs` | `unpacked::decapsulate` | Added F* requires using `mk_usize 2` (rank 2). |
| `src/mlkem768.rs` | `unpacked::decapsulate` | Added F* requires using `mk_usize 3` (rank 3). |
| `src/mlkem1024.rs` | `unpacked::decapsulate` | Added F* requires using `mk_usize 4` (rank 4). |

## Pre-existing extractor bug (out of Lane E scope)

The Mlkem*.Portable.Unpacked.fst / Mlkem*.Avx2.Unpacked.fst modules currently fail to BUILD on this checkout due to an **extractor bug** that is independent of my flip: the freshly extracted `Libcrux_ml_kem.Vector.Neon.Vector_type.fsti` contains duplicate `noeq` qualifiers (lines 6 and 8 — a `noeq` is emitted twice in a row before the `type` keyword). This is a parse-level error that `--admit_smt_queries true` (the mechanism backing `ADMIT_MODULES`) does NOT mask. The error blocks parsing for every module that has `let open Libcrux_ml_kem.Vector.Neon` in its `.fsti` (Mlkem*.{Portable,Avx2}.Unpacked).

The file is not committed at HEAD (regenerated by hax extract on demand). Pre-existing `.checked` files in `.fstar-cache/checked/` indicate that an earlier extractor revision did not emit this duplicate. **The required modules per task contract — `Ind_cca.fst`, `Mlkem512.fst`, `Mlkem1024.fst`, `Ind_cca.Unpacked.fst` — do not depend on Neon and pass cleanly.** Recommend a follow-up to add `proofs/fstar/extraction-patches/Libcrux_ml_kem.Vector.Neon.Vector_type.fsti.patch` to drop the duplicate `noeq`. Out of scope here.

## Final verification status (all green)

| Module | Status | Time |
|---|---|---|
| `Libcrux_ml_kem.Ind_cca.Unpacked` | Verified | 77.7s (rlimit 400 + split_queries — 162 sub-queries) |
| `Libcrux_ml_kem.Ind_cca` | Verified | 1.1s |
| `Libcrux_ml_kem.Mlkem512` | Verified | 0.7s |
| `Libcrux_ml_kem.Mlkem1024` | Verified | 0.7s |
| `Libcrux_ml_kem.Ind_cca.Instantiations.Portable.Unpacked` | Verified | (after re-extract) |
| `Libcrux_ml_kem.Ind_cca.Instantiations.Avx2.Unpacked` | Verified | (after re-extract) |

`Libcrux_ml_kem.Ind_cca.Unpacked.fst` is **no longer in `ADMIT_MODULES`** in `libcrux-ml-kem/proofs/fstar/extraction/Makefile`.

## R-rule audit

- R1 (no remote push, no PR, no force-push): respected — branch local only.
- R2 (no new admits, no `--admit_smt_queries true`, no `admit ()` calls in source): respected — the only `admit ()` calls in extracted `Unpacked.fst` are hax-emitted `panic_free` ensures-stubs, which are the standard panic_free pattern (per CLAUDE memory: "panic_free IS the real middle step").
- R3 (60-min budget per fn): respected — encapsulate flipped immediately, decapsulate flipped after one rlimit bump (~10 min total).
- R5 (no edits to `src/vector/traits.rs`): respected — file untouched.
- R7 (source-only edits in `src/*.rs` and `proofs/fstar/extraction/Makefile`): respected — edits to `src/ind_cca.rs`, `src/ind_cca/instantiations.rs`, `src/ind_cca/instantiations/avx2.rs`, and `Makefile`. Extracted `.fst[i]` files modified only via `python3 hax.py extract`.
- R8 (use `python3 hax.py extract`): respected — never invoked `cargo hax` directly.
- R9 (real verification, no admit-shuffling): respected — Unpacked.fst left ADMIT_MODULES, everything that used to depend on the admit had its requires propagated honestly so the new bound chains transparently.
- R11 (commits prefixed `agent-mlkem:`): see commit SHA below.

## Files changed
- `libcrux-ml-kem/src/ind_cca.rs` — added `ToProp` import, `panic_free` status on `unpacked::{encapsulate,decapsulate}`, new `is_bounded_polynomial_vector` requires + rlimit bump on `unpacked::decapsulate`.
- `libcrux-ml-kem/src/ind_cca/instantiations.rs` — propagated bound to `decapsulate`.
- `libcrux-ml-kem/src/ind_cca/instantiations/avx2.rs` — propagated bound to `decapsulate_avx2` and outer `decapsulate`.
- `libcrux-ml-kem/src/mlkem512.rs` / `mlkem768.rs` / `mlkem1024.rs` — propagated bound to top-level `unpacked::decapsulate`.
- `libcrux-ml-kem/proofs/fstar/extraction/Makefile` — dropped `Libcrux_ml_kem.Ind_cca.Unpacked.fst` from `ADMIT_MODULES`.
- `libcrux-ml-kem/proofs/fstar/extraction/Libcrux_ml_kem.Ind_cca.Unpacked.fst` — re-extracted (panic_free body, hax-emitted `admit ()` panic-freedom stubs at function ends).
- Re-extracted .fst[i] artifacts for Instantiations and Mlkem*.{Portable,Avx2}.Unpacked also touched.

## Final commit SHA
(to be filled)
